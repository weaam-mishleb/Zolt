# Zolt backend — Python dependencies
#
# PINNED to the locally-tested set. Ranges (>=) bit us in production: a
# "clear build cache" deploy on Render resolved to a brand-new, untested
# stack (uvicorn 0.51 / starlette 1.3) — pin so every deploy is reproducible.
fastapi==0.136.3
starlette==1.2.1
uvicorn[standard]==0.48.0
SQLAlchemy==2.0.50
PyMySQL==1.2.0
pydantic==2.13.4
pydantic-settings==2.14.1
bcrypt==5.0.0
PyJWT==2.13.0
APScheduler==3.11.2
httpx==0.28.1        # GitHub Actions workflow_dispatch (admin ETL trigger)
python-multipart==0.0.20  # FastAPI UploadFile — the OFF photo-contribution endpoint
