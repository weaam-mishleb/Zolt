# Zolt — תיעוד בדיקות מלא

**מצב אחרון מאומת:** ‏113/113 בדיקות יחידה · ‏10/10 תרחישי אינטגרציה (TC-1..TC-10) · שיא זיכרון ETL נמדד: **89MB** (יעד: <100MB)

המערכת נבדקת בשלוש שכבות משלימות — כל שכבה תופסת סוג כשל אחר:

| שכבה | מה בודקת | איך מריצים |
|---|---|---|
| **Unit** (113) | הלוגיקה העסקית הטהורה — ללא DB, דטרמיניסטי, ‏<1s | `make test` |
| **Integration** (10) | המערכת בשלמותה — API אמיתי + MySQL אמיתי + קבצים אמיתיים | `python -m scripts.run_test_plan` |
| **Operational** | בריאות רציפה בפרודקשן — DB, זמינות, תקציבי זמן | `GET /health` (נבדק כל 5 דק' ע"י UptimeRobot) |

---

## 1. בדיקות יחידה — `backend/tests/` (113)

מרוכזות בליבה שבה באג עולה למשתמש כסף אמיתי: מנוע ההשוואה ונרמול הנתונים. כולן רצות **ללא מסד נתונים** (הנתונים מוזרקים), ולכן מהירות ודטרמיניסטיות.

### `test_comparison.py` — מנוע ההשוואה והדירוג (10 בדיקות)

| בדיקה | מה מאומת | דרישת SRS |
|---|---|---|
| `test_winner_is_cheapest_complete_and_incomplete_excluded` | המנצח הוא הסניף **השלם** הזול ביותר; סניף חסר-מוצר מוצג אך לא מדורג (`rank=None`), עם רשימת החוסרים וסכום חלקי; **פער ה-% מהזול** מחושב נכון (0% למנצח, ‏16.7% לשני, ‏None לחלקי) | FR-4.1, FR-4.2, FR-4.3, FR-7.2 |
| `test_no_complete_store_yields_no_winner_but_still_lists_stores` | כשאף סניף אינו שלם — אין מנצח, אך הסניפים מוצגים ממוינים לפי (הכי פחות חוסרים, ואז הזול) | FR-4.3 |
| `test_cheapest_barcode_per_name_wins_in_store` | כששני ברקודים של אותו מוצר קיימים באותו סניף — הזול מייצג | FR-6.1/6.2 (סטיה מוצהרת: התאמת שם) |
| `test_results_capped_at_10_with_chain_mix` | תקרת 10 סניפים עם ייצוג לכל רשת — הזול-של-כל-רשת נשמר, והמנצח תמיד ראשון | FR-4.2, FR-7.1 |
| `test_size_tokens_distinguish_multipack_from_single` | חתימת הגדלים מונעת ממארז 10×25 גרם "לשאול" מחיר של שקית בודדת | FR-6.1 (התאמה מדויקת) |
| `test_prominent_tokens_skips_sizes_units_and_stopwords` | הטוקניזציה מחלצת מילות-מותג ומדלגת על גדלים/יחידות/מילות עצירה (`קוקה קולה שישיה 1.5 ליטר → [קוקה, קולה]`) | FR-2.1, FR-6.2 |
| `test_quantity_multiplies_line_total` | כמות מכפילה נכון את שורת המחיר (3 × 4.50 = 13.50) | FR-4.1 |
| `test_phantom_twin_store_is_collapsed` | כפיל "רפאים" מהפיד (אותו סניף בשני קודים — בלי כתובת, מחירון ישן) מאוחד לסניף האמיתי, והמחיר הישן לא זוכה | FR-7.1 (איכות נתוני מקור) |
| `test_same_name_branches_with_distinct_addresses_are_kept` | שני סניפים אמיתיים עם אותו שם וכתובות שונות — לא מאוחדים | FR-7.3 |
| `test_size_tokens_distinguish_multipack_from_single` (מורחבת) | חתימת גדלים דו-כיוונית — מארז `4*55` לא מתאים לשקית `55` | FR-6.1 |
| `test_head_token_filter_keeps_variants_and_rejects_different_products` | מסנן "מילת הראש": שומר `אוכמניות`↔`אוכמניות טריות` ו-`מגבון`↔`מגבוני` (נרמול אותיות סופיות), חוסם `שמן קוקוס`↔`קוקוס` | FR-6.2 (דיוק ההתאמה) |

### `test_cities.py` — נרמול ערים חוצה-רשתות (33 בדיקות פרמטריות)

הרשתות שולחות ערים בפורמטים שונים: שופרסל — **שמות** (עם וריאנטים), רמי לוי ואושר עד — **קודי למ"ס** מספריים. בלי נרמול, הסינון לפי עיר פשוט לא עובד.

| קבוצה | דוגמאות מאומתות |
|---|---|
| וריאנטי שמות → קנוני (‏6) | `תל אביב-יפו` / `תלאביב` / `"תל אביב"` ← תל אביב; `פתח-תקוה` ← פתח תקווה |
| קודי למ"ס → שם (‏4) | `5000` ← תל אביב; `3000` ← ירושלים; `9000` ← באר שבע; `874` ← מגדל העמק |
| קוד ריק/אפס → גזירה משם הסניף (‏3) | `"", "רגר באר שבע"` ← באר שבע; `"רמות"` ← ירושלים (שכונה) |
| שם לא מוכר עובר נקי, ללא מיפוי שגוי (‏5) | `גבעתיים` ← גבעתיים; קוד לא מוכר ← None |
| איחוד כפילויות רווחים/מקפים (‏15) | `אור-יהודה` ← אור יהודה; `כפר-סבא`/`כפר סבא צפון` ← כפר סבא; `יוקנעם`/`יקנעם עילית` ← יקנעם; `קריתגת` ← קרית גת |

### `test_basket_validation.py` — ולידציית API ברמת ה-endpoint (5 בדיקות)

רצות דרך TestClient אך **ללא DB** — כל הבקשות נדחות (או נענות) לפני שרצה שאילתה.

| בדיקה | מה מאומת | דרישת SRS |
|---|---|---|
| `test_compare_rejects_more_than_50_items` | סל של 51 שורות → ‏400 עם הודעה ברורה | אילוץ 50 הפריטים |
| `test_summary_rejects_more_than_50_items` | אותה תקרה גם ב-`/basket/summary` | אילוץ 50 הפריטים |
| `test_summary_rejects_negative_quantity` | כמות שלילית → ‏400 | FR-3 (ולידציה) |
| `test_summary_empty_basket_is_empty_not_error` | סל ריק → ‏200 עם סיכום ריק, לא שגיאה | FR-3.6 |
| `test_compare_still_rejects_empty_basket` | ‏regression — סל ריק בהשוואה עדיין ‏400 | FR-4 |

### `test_tokenization.py` — פונקציות הטוקניזציה הטהורות (46 בדיקות פרמטריות)

מכסות את לב מנוע ההתאמה — כל מקרה הוא התנהגות מובחנת, לא כפילות.

| קבוצה | מה מאומת | דרישת SRS |
|---|---|---|
| `prominent_tokens` (‏15) | חילוץ מילות-מותג + דילוג על גדלים/יחידות/אחוזים/מילות עצירה, פיצול על `- / ,`, תקרת 4, None-safe | FR-2.1, FR-6.2 |
| `size_tokens` (‏15) | חתימת גדלים: תבניות מארז (`4*55`, `3X250`, `3×250`), התעלמות מספרה בודדת, dedup, תקרת 2 | FR-6.1 |
| `_head_ok` (‏11) | מסנן מילת-ראש כולל נרמול אותיות סופיות (`מגבון`↔`מגבוני`), חסימת מוצר שונה (`שמן קוקוס`) | FR-6.2 |
| `_norm_name` (‏5) | איחוד רווחים/טאבים, strip, None→"" | — |

### `test_money_and_search.py` — עיגול כספי + בניית ביטוי חיפוש (19 בדיקות)

| קבוצה | מה מאומת | דרישת SRS |
|---|---|---|
| `_money` (‏10) | עיגול ל-2 ספרות ROUND_HALF_UP (‏18.005→18.01, נמנע ממלכודת ה-float של 2.675), None-safe | FR-4.1 |
| `_boolean_expr` (‏9) | בניית ביטוי FULLTEXT (`חלב תנו → +חלב* +תנו*`), הסרת אופרטורים/גרשיים/סוגריים, קלט ריק | FR-2.1/2.2 |

**הרצה:** `make test` או `SCHEDULER_ENABLED=false python -m pytest backend/tests -q` → `113 passed`.

---

## 2. בדיקות אינטגרציה — Test Plan ‏TC-1..TC-10 (`scripts/run_test_plan.py`)

רצות דרך **FastAPI TestClient מול MySQL אמיתי** — מפעילות את כל השרשרת: ולידציית Pydantic → routers → services → SQL. תרחישי ה-DB ההרסניים (TC-6) עטופים בטרנזקציה עם **rollback** כדי לא לזהם נתונים.

| TC | תרחיש | שיטה | תוצאה מצופה | תוצאה אחרונה |
|---|---|---|---|---|
| TC-1 | סל תקין — סכום ודירוג | ‏3 מוצרים ×[2,1,1] בתל אביב → `POST /basket/compare` | ‏200; סניפים שלמים ממוינים מהזול ליקר | ✅ PASS — ‏19 סניפים, מנצח 105 |
| TC-2 | כמות שלילית | `quantity: -1` | ‏400 + הודעה ברורה | ✅ PASS |
| TC-3 | סל ריק | `items: []` | ‏400 + הודעה ברורה | ✅ PASS |
| TC-4 | מוצר חסר לא זוכה | עיר בה סניף חסר פריט | הסניף החלקי `rank=None`; מנצח שלם בלבד | ✅ PASS (אופקים) |
| TC-5 | **תקציב זיכרון ETL** | ‏ETL מלא (85MB, ‏489K שורות) תחת `/usr/bin/time -l` | שיא RSS ‏< 100MB | ✅ PASS — **89MB, ‏29.6s** |
| TC-6 | ‏Upsert בלי כפילות | שינוי מחיר + ‏upsert חוזר לאותו (product,store) | עדיין שורה אחת; המחיר עודכן (rollback בסוף) | ✅ PASS |
| TC-7 | שורת CSV פגומה | קובץ 2,000 שורות עם שורה משובשת באמצע | ‏1,999 שורות נקראות — השורה הפגומה מדולגת, לא מפילה | ✅ PASS |
| TC-8 | חיפוש עברית במונח אנגלי | `GET /products/search?q=milk` | ‏200 + רשימה ריקה (graceful, לא שגיאה) | ✅ PASS |
| TC-9 | עיר בלי סניפים | השוואה בעיר ללא סניפים | ‏200 + `message` מסודר, לא 500 | ✅ PASS |
| TC-10 | ‏Login → JWT | פרטי admin תקינים | ‏200 + טוקן תקף; תפוגה == ‏`JWT_EXPIRE_HOURS` (24h, ‏SRS FR-5.2) | ✅ PASS — ‏24.0h |

### שני מצבי הרצה

```bash
# מול ה-DB הענני (ברירת המחדל של .env) — TC-5 מדלג בחיבור ביתי איטי (צוואר בקבוק רשת, לא זיכרון)
python -m scripts.run_test_plan                      # → PASS=9, SKIP=1

# מול DB מקומי (Docker) — המדידה המלאה, כולל TC-5
docker compose up -d
DATABASE_URL="mysql://zolt:zolt_pass@127.0.0.1:3306/zolt" SCHEDULER_ENABLED=false \
  python -m scripts.run_test_plan                    # → PASS=10
```

‏TC-5 מבחין במכוון בין **כשל הדרישה** (זיכרון חרג → FAIL) לבין **כשל מדידה סביבתי** (‏timeout רשתי מול ענן → SKIP) — בדיקה שמסמנת FAIL על רשת איטית הייתה משקרת.

---

## 3. השכבה התפעולית — בדיקות רציפות בפרודקשן

- **`GET /health`** — מאמת חיבור DB חי עם **תקציב זמן קשיח של 5 שניות**: ‏DB תקין → ‏200 `{"db":"up"}`; תקוע → ‏503 `{"db":"timeout"}` מהיר במקום בקשה תלויה (לקח מתחקיר "אינסטנס הזומבי" — בקשה תלויה חסמה shutdown והפילה את השירות). מנוטר ע"י UptimeRobot כל 5 דקות.
- **`etl_jobs`** — כל ריצת ETL מתועדת: סטטוס, ‏% התקדמות, זמנים, שגיאות. זיהוי ריצה תקועה (stale) אוטומטי אחרי 15 דקות ללא עדכון. נצפה בפאנל הניהול (FR-1.5, FR-5.4).

---

## 4. עקיבות דרישות ← בדיקות (Traceability)

| דרישה | בדיקות מכסות |
|---|---|
| FR-1.2 (ניקוי ונרמול) | ‏33 בדיקות הערים · TC-7 |
| FR-1.3 (‏Upsert) | TC-6 |
| FR-1.5 (תיעוד ריצות) | ‏`etl_jobs` + פאנל היסטוריה |
| FR-2.1/2.2 (חיפוש) | ‏token tests · TC-8 |
| FR-4.1 (סכום סל) | ‏3 בדיקות יחידה · TC-1 |
| FR-4.2 (דירוג + ‏% מהזול) | ‏2 בדיקות יחידה · TC-1 |
| FR-4.3 (מוצרים חסרים) | ‏2 בדיקות יחידה · TC-4 |
| FR-5.1/5.2 (‏Auth + JWT 24h) | TC-10 |
| ‏NFR זיכרון ETL | TC-5 — נמדד: ‏89MB |
| FR-3.6 (סיכום עגלה) + אילוץ 50 פריטים | ‏5 בדיקות הוולידציה |
| עמידות קלט (מעבר ל-happy path) | TC-2, TC-3, TC-8, TC-9 + בדיקות הוולידציה |

## 5. מגבלות ידועות (מוצהר)

- **Frontend ללא בדיקות אוטומטיות** — החלטת היקף מודעת ב-MVP; הלוגיקה העסקית כולה בצד השרת והיא המכוסה. ה-UI מאומת ידנית + ‏`vite build` בכל שינוי.
- ‏**TC-5 דורש DB מקומי/CI** למדידה מלאה (מול ענן — SKIP מוסבר).
- ‏Coverage לא נמדד באחוזים במכוון — הבדיקות ממוקדות סיכון, לא מרדף מספרים.
