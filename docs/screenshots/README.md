# Screenshots

Drop the following PNGs here (referenced from the root `README.md`):

| File | What to capture |
|------|-----------------|
| `home.png` | The home page — hero + search bar + empty basket |
| `search.png` | The autocomplete search dropdown with results |
| `comparison.png` | The main comparison table (winner highlighted, horizontal scroll) |
| `admin.png` | The admin panel (`/admin`) — login or the dashboard |

Recommended width ~1400px, PNG, light mode.
