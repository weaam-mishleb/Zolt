# Zolt ETL — Python dependencies
# (parsing uses the stdlib csv module — no pandas — to keep peak RAM low)
SQLAlchemy>=2.0
PyMySQL>=1.1
pydantic-settings>=2.3
# Pinned, like everything else here. `kaggle>=1.6` let CI float onto the 2.x
# rewrite, which talks to api.kaggle.com and reports a failed dataset lookup as
# a bare "403 Forbidden for url: .../GetDatasetMetadata" — no reason, no ref.
# 1.6.17 said "Permission 'datasets.get' was denied", which is what finally
# pointed at the dataset instead of the credentials. 2.2.0 works correctly; it
# is just far less forthcoming when something is wrong.
# 2.2.2 verified against the live dataset before bumping, on both paths that
# matter: a present file downloads, and a missing one still reports a greppable
# "404 Client Error: Not Found" — which the workflow's skip logic depends on.
kaggle==2.2.2      # automated dataset download (scheduler)
